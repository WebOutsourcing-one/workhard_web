# Base URL of Supabase Edge Functions for WorkHard.
# Used to fetch the latest version metadata and serve the installer download.
NEXT_PUBLIC_SUPABASE_FUNCTIONS_URL=https://<your-project-ref>.supabase.co/functions/v1
